let initialState = [ {name: 'profile'}, {name: 'messages'}, {name: 'news'}, {name: 'users'}, {name: 'options'} ]

const navbarReducer = (state = initialState)=>{
    return state
}

export default navbarReducer